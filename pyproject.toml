[project]
name = "snnib"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "lukas.steinwender99@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "brian2>=2.9.0",
    "numpy>=1.0,<2.0",
]

[project.scripts]
brian2_sim = "snnib.commands.simulate_brian2.py:main"

[build-system]
requires = ["uv_build>=0.8.15,<0.9.0"]
build-backend = "uv_build"
